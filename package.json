{
  "name": "dots-boxes",
  "source": "src/index.html",
  "license": "MIT",
  "scripts": {
    "start": "rm -rf dist .parcel-cache && parcel --no-cache",
    "build": "parcel build"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.0.1",
    "@parcel/transformer-webmanifest": "^2.0.0",
    "parcel": "^2.0.0"
  },
  "dependencies": {
    "@galpop/themetoggler": "^1.1.3",
    "lodash.clonedeep": "^4.5.0"
  }
}
